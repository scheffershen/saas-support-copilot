# Assigning tickets

A `support_lead` can assign any open ticket to a `support_agent`. Assignment fails if
the target user's account is deactivated — reactivate the user first if you need to
assign work to them.
