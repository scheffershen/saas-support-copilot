# Creating a ticket

1. Click **New ticket**.
2. Enter a title and description.
3. Submit. The ticket starts in the `open` status with no assignee.

## Statuses

`open` -> `in_progress` -> `resolved` -> `closed`. Only a `support_agent` or
`support_lead` can change a ticket's status.
